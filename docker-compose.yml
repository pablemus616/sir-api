version: '3.8'

services:
  sir-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sir_api
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
